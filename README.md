# logseq-obsidian-sync

Watches a Logseq graph for smartpen pages and mirrors them as native Obsidian
markdown into a vault, stripping outliner-only artefacts.

Usage:
    npm install
    LOGSEQ_GRAPH=~/LogseqGraph OBSIDIAN_VAULT=~/vault npm start
